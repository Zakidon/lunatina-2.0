---
import Layout from '../layouts/BaseLayout.astro';
---

<Layout title="Контакты | Lunatina">
  <main class="py-16">
    <div class="container mx-auto px-4 max-w-2xl">
      <h1 class="text-4xl font-bold text-center mb-12">Свяжитесь с нами</h1>
      
      <div class="bg-white dark:bg-gray-800 rounded-xl shadow-md p-8">
        <form id="contactForm">
          <div class="mb-6">
            <label class="block text-lg font-medium mb-2" for="name">Ваше имя</label>
            <input 
              type="text" 
              id="name"
              required
              placeholder="Как к вам обращаться?"
              class="w-full px-4 py-3 border border-gray-300 dark:border-gray-700 rounded-lg focus:ring-2 focus:ring-violet-500 focus:border-transparent bg-white dark:bg-gray-700"
            />
          </div>
          
          <div class="mb-6">
            <label class="block text-lg font-medium mb-2" for="email">Email</label>
            <input 
              type="email" 
              id="email"
              required
              placeholder="Ваш email для обратной связи"
              class="w-full px-4 py-3 border border-gray-300 dark:border-gray-700 rounded-lg focus:ring-2 focus:ring-violet-500 focus:border-transparent bg-white dark:bg-gray-700"
            />
          </div>
          
          <div class="mb-6">
            <label class="block text-lg font-medium mb-2" for="subject">Тема</label>
            <input 
              type="text" 
              id="subject"
              required
              placeholder="Кратко о чём ваше сообщение"
              class="w-full px-4 py-3 border border-gray-300 dark:border-gray-700 rounded-lg focus:ring-2 focus:ring-violet-500 focus:border-transparent bg-white dark:bg-gray-700"
            />
          </div>
          
          <div class="mb-6">
            <label class="block text-lg font-medium mb-2" for="message">Сообщение</label>
            <textarea 
              id="message"
              required
              rows="5"
              placeholder="Напишите ваше сообщение подробнее"
              class="w-full px-4 py-3 border border-gray-300 dark:border-gray-700 rounded-lg focus:ring-2 focus:ring-violet-50 focus:border-transparent bg-white dark:bg-gray-700"
            ></textarea>
          </div>
          
          <button type="submit" class="w-full py-4 bg-violet-60 hover:bg-violet-700 text-white font-bold rounded-lg transition-colors">
            Отправить сообщение
          </button>
        </form>
        
        <div id="successMessage" class="mt-6 p-4 bg-green-100 dark:bg-green-900 text-green-700 dark:text-green-200 rounded-lg hidden">
          Спасибо за ваше сообщение! Мы свяжемся с вами в ближайшее время.
        </div>
        
        <div class="mt-8 pt-8 border-t border-gray-200 dark:border-gray-700">
          <h2 class="text-xl font-bold mb-4">Альтернативные способы связи</h2>
          <ul class="space-y-3">
            <li class="flex items-center">
              <span class="mr-3">📧</span>
              <a href="mailto:info@lunatina.ru" class="text-violet-600 hover:underline">info@lunatina.ru</a>
            </li>
            <li class="flex items-center">
              <span class="mr-3">💬</span>
              <a href="https://t.me/lunatina" target="_blank" class="text-violet-600 hover:underline">Telegram</a>
            </li>
          </ul>
        </div>
      </div>
    </div>
 </main>
  
  <script>
    const form = document.getElementById('contactForm');
    const successMessage = document.getElementById('successMessage');
    
    form.addEventListener('submit', (e) => {
      e.preventDefault();
      
      // Валидация формы
      let isValid = true;
      const inputs = form.querySelectorAll('input[required], textarea[required]');
      
      inputs.forEach(input => {
        if (!input.value.trim()) {
          isValid = false;
          input.classList.add('border-red-500');
        } else {
          input.classList.remove('border-red-500');
        }
      });
      
      if (!isValid) {
        return;
      }
      
      // В реальном приложении здесь была бы отправка данных на сервер
      // Сейчас просто покажем сообщение об успехе
      
      form.reset();
      successMessage.classList.remove('hidden');
      
      // Скроем сообщение через 5 секунд
      setTimeout(() => {
        successMessage.classList.add('hidden');
      }, 5000);
    });
    
    // Убираем стили ошибки при вводе
    const inputs = form.querySelectorAll('input, textarea');
    inputs.forEach(input => {
      input.addEventListener('input', () => {
        if (input.classList.contains('border-red-500')) {
          input.classList.remove('border-red-500');
        }
      });
    });
  </script>
</Layout>